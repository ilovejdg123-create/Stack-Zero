{
  "name": "stack-zero",
  "version": "40.6.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/coach.js && node --check api/azure-tts.js && node --check api/gemini-tts.js"
  }
}
