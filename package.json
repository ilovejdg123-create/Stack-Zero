{
  "name": "stack-zero",
  "version": "1.0.1",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/coach.js && node --check api/azure-tts.js && node --check api/gemini-tts.js && node --check api/account.js && node --check api/sync.js && node --check api/health.js && node --check api/_cloud.js"
  }
}
